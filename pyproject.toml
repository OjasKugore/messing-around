[project]
name = "python-playground"
version = "0.1.0"
description = "A monorepo for Python experiments — a database migration CLI and a RAG pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain-experimental>=0.4.2",
    "langsmith>=0.11.0",
    "rank-bm25>=0.2.2",
]

[tool.uv.workspace]
members = ["clitool", "rag"]
